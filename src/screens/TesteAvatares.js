import { icon } from '../assets/index';
import * as React from 'react';
import { Avatar } from 'react-native-paper';
import {Text} from 'react-native';

const TesteAvatares = () => (
 <Avatar.Image size={24} source={icon} />
);

export default TesteAvatares